export default function LoginPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center gap-4 bg-slate-900 p-10 text-center text-white">
      <h1 className="text-3xl font-semibold">Rental Ops Console</h1>
      <p className="max-w-sm text-sm text-slate-200/80">
        Authentication will be wired up in later tasks. For now, this placeholder
        keeps the route healthy.
      </p>
    </div>
  );
}
